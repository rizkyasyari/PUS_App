<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.spusapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application xmlns:tools="http://schemas.android.com/tools"
        android:allowBackup="true"
        android:icon="@drawable/ic_pus_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_pus_app"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme"
        tools:replace="android:allowBackup">
        <activity android:name=".DaftarActivity"></activity>
        <activity android:name=".TutorialActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".MenuJenisPembayaranActivity" />
        <activity android:name=".OtpActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Transaction.ProductDetailActivity" />
        <activity android:name=".Transaction.ProductOrderActivity" />
        <activity android:name=".Transaction.ProductListActivity" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>